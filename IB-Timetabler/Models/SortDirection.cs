namespace IB_Timetabler.Models {
    
    /// Enumeration for the possible sort directions of a ColumnDefinition 
    public enum SortDirection {
        NotSet,
        Ascending,
        Descending
    }
}