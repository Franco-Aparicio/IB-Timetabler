@* The date component in the top right corner of the MainLayout *@

@dateTimeNow.ToString("MM/dd/yyyy")

@code {
    DateTime dateTimeNow;

    protected override void OnInitialized() {
        base.OnInitialized();
        dateTimeNow = DateTime.Now;
    }
}